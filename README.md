# HTML-Structure-and-Basic-Server-Interaction
A Node.js and Express.js web application that demonstrates form handling, server-side rendering, and dynamic HTML generation using EJS. Users can submit their details through a web form, which is processed by the Express server and displayed dynamically using EJS templates.
